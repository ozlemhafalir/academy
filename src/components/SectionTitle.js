import React from 'react';

function SectionTitle(props) {
    return (
        <h2 style={{marginTop: "2rem", marginBottom: "1rem", textAlign: "center"}}>{props.title}</h2>
    );
}

export default SectionTitle;
